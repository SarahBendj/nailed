/** Stub: Hall is now accessed via PrismaService in hall.service. This stub exists for availability/review that still import it. */
export const Hall = {
  findAll: () => Promise.resolve([] as any[]),
  findOne: (_id: number) => Promise.resolve(null as any),
  Update: (_id: number, _data: any) => Promise.resolve(null as any),
};
